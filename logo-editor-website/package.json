{
  "name": "logo-editor-website",
  "version": "1.0.0",
  "description": "",
  "main": "scripts.js",
  "scripts": {
    "build:js": "esbuild main.js --bundle --outfile=main.bundle.js && esbuild scripts.js --bundle --outfile=dist/scripts.js",
    "build:css": "esbuild styles.css --bundle --outfile=dist/styles.css",
    "build": "npm run build:js && npm run build:css"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vercel/analytics": "^1.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.5"
  }
}
